import React from 'react';

/**
 * Logic to render single label and text input
 */
export default ( { input } ) => {
    return (
        <div>
            <input {...input}/>
        </div>
    );
}
